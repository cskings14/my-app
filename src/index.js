import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';
import './styles/global.css';


// The App comes from the components folder

ReactDOM.render(<App />, document.getElementById("root")); // this will render the element inside the root div in the index.html file
